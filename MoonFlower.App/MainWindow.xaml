<Controls:MetroWindow 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MoonFlower.App"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:vm="clr-namespace:MoonFlower.ViewModel;assembly=MoonFlower.ViewModel"
        mc:Ignorable="d"
        Title="MoonFlower" d:DesignHeight="700" d:DesignWidth="525"
        SaveWindowPosition="True">

    <Controls:MetroWindow.DataContext>
        <vm:MainViewModel />
    </Controls:MetroWindow.DataContext>
    <Grid x:Name="Main" ShowGridLines="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <StackPanel x:Name="Header" Grid.Row="0">
            <ListBox x:Name="AccountSelector" Height="50"></ListBox>
            <StackPanel x:Name="TootBox">
                <TextBox x:Name="TootText" Height="100" Text="{Binding InputText}"/>
                <StackPanel x:Name="TootActions" Orientation="Horizontal">
                    <Button x:Name="TootButton" Content="Toot!" />
                </StackPanel>
            </StackPanel>
        </StackPanel>
        <ItemsControl Grid.Row="1" ItemsPanel="{DynamicResource TimelinePaneItems}" ItemTemplate="{DynamicResource TimelinePaneControl}">
            <ItemsControl.Resources>
                <ItemsPanelTemplate x:Key="TimelinePaneItems">
                    <UniformGrid Rows="1"/>
                </ItemsPanelTemplate>
                <DataTemplate x:Key="TimelinePaneControl">
                    <Border>
                        <ItemsPresenter />
                    </Border>
                </DataTemplate>
            </ItemsControl.Resources>

        </ItemsControl>
    </Grid>
</Controls:MetroWindow>
